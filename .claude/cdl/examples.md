# Examples:
- ALLOC:0@HOT = No allocations inside hot path.
- PATCH:SAFE = Minimal safe modification.
- API:KEEP = Preserve public API.
- PROFILE:CPU = Run CPU profiling.
- TEST:UNIT = Execute unit tests only.